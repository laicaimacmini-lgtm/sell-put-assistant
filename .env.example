# Local API proxy configuration. Never commit real secrets.
OPTIONS_DATA_PROVIDER=mock
PORT=8787

# Provider tokens are server-side only. Leave blank until configured locally.
TRADIER_TOKEN=
MARKETDATA_TOKEN=
ALPACA_KEY=
ALPACA_SECRET=
